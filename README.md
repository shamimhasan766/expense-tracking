installation proccess:

1. clone the github project.
2. setup env with mysql database i have added in the github in the root folder. (expense_tracker.sql)
3. run "composer run dev" or "php artisan serve" for locally run (if dont show content with php artisan serve use composer run dev.. cause i have used react for frontend)
